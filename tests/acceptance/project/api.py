"""Sample Python module for autodoc coexistence checks."""

from __future__ import annotations


def greet(name: str = "world") -> str:
    """Return a friendly greeting for *name*."""
    return f"Hello, {name}!"
